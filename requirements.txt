# Core — required for the YouTube/PDF ingestion pipeline
yt-dlp
markitdown

# Convenience — used for one-off spreadsheet/data inspection outside the
# excel MCP server; not required for day-to-day command operation
openpyxl
pandas

# Required for scripts/oura_analyze.py (/oura-analyze) — correlations,
# FDR-corrected group comparisons, cross-validated regression/random forest
numpy
scipy
statsmodels
scikit-learn
pyyaml

# Optional — local fallback transcription (scripts/whisper_transcribe.py) for when
# YouTube's caption-serving endpoint is IP-blocked but video/audio download still works.
# As of 2026-07-27 this shells out to a locally-built whisper.cpp binary (native ARM64,
# see buglog.md) rather than a pip package — no Python ML dependency needed here, just
# ffmpeg on PATH and the compiled binary + model under
# %LOCALAPPDATA%\HealthVault-Tools\whisper-cpp\ (built from ggml-org/whisper.cpp source,
# not redistributed here). Not required for normal operation.

# Dev tooling — lint/format (ruff), schema validation (pandera), fast dataframes
# for larger corpora (polars, e.g. the raw-transcript metadata as it scales up).
ruff
pandera
polars

# Occasional use only — history maintenance (see README "History rewrites").
# Not needed for normal operation; install on demand when actually rewriting git history.
# git-filter-repo
